name: marenero
description: .

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  # Flutter
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # Dart

  # Firebase
  firebase_core: ^1.0.3
  cloud_firestore: ^1.0.5

  # Third party

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
